import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{join}from'node:path';import test from'node:test';const source=readFileSync(join(process.cwd(),'services','forecast','app','main.py'),'utf8');test('python service preserves stored forecast contracts',()=>{for(const x of ['/forecast/run','/backtest/run','/models','/health','forecast_result','model_version'])assert.match(source,new RegExp(x));assert.doesNotMatch(source,/v_test_actual|usage_history/);});
