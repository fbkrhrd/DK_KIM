import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{join}from'node:path';import test from'node:test';const s=readFileSync(join(process.cwd(),'supabase','migrations','20260828000900_add_safety_stock_purchase_recommendation.sql'),'utf8');test('uses SQL safety stock and MOQ pack rounding',()=>{for(const x of ['v_model_performance','v_effective_lead_time','sqrt','greatest','ceil','NO_FORECAST','NO_SERVICE_LEVEL'])assert.match(s,new RegExp(x));});
