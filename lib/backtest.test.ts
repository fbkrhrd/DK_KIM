import assert from 'node:assert/strict';import{readFileSync}from'node:fs';import{join}from'node:path';import test from'node:test';const sql=readFileSync(join(process.cwd(),'supabase','migrations','20260828000600_add_backtest_champion_models.sql'),'utf8');test('scores stored forecasts only against test actuals',()=>{assert.match(sql,/core\.forecast_result f join core\.v_test_actual/);assert.doesNotMatch(sql,/raw\.usage_history|run_baseline_forecast/);});test('stores metrics ranks champions and manual audit trail',()=>{for(const x of ['wape','mape','bias','rmse','mae','champion_model_history','MANUAL_REASON_REQUIRED','CHAMPION_MANUAL_SELECTED'])assert.match(sql,new RegExp(x,'i'));});
