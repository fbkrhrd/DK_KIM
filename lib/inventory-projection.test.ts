import assert from'node:assert/strict';import{readFileSync}from'node:fs';import{join}from'node:path';import test from'node:test';const s=readFileSync(join(process.cwd(),'supabase','migrations','20260828000800_add_inventory_projection.sql'),'utf8');test('uses stored champion forecast and projection components',()=>{for(const x of ['v_champion_model','v_forecast_result','receipts-confirmed_sales-soft_allocation-predicted_qty','NO_INVENTORY_DATA','NO_FORECAST','NO_LEADTIME'])assert.match(s,new RegExp(x));});
