# superSCM 디자인 시스템

## 1. 디자인 방향

첨부된 SCM Pro 레퍼런스의 방향을 현재 조달 계획 애플리케이션에 맞게 적용했습니다.

- 밝고 중립적인 표면을 사용하는 엔터프라이즈 운영 도구
- 왼쪽 고정 내비게이션과 상단 고정 헤더
- 짙은 네이비를 주요 브랜드·행동 색상으로 사용
- 얇은 경계선, 낮은 그림자, 8px 중심의 절제된 카드 모서리
- 정보 밀도가 높은 표와 KPI를 우선하는 레이아웃
- 기능 구조와 기존 한글 콘텐츠는 유지하고 시각 언어를 통일

## 2. 레이아웃

```text
┌────────────── 256px 고정 사이드바 ──────────────┬──────────────────────────────┐
│ SCM 브랜드                                     │ 64px 고정/스티키 상단 헤더   │
│ 새 업무 / 워크플로우 내비게이션                ├──────────────────────────────┤
│ 분석                                            │ 페이지 제목·KPI·콘텐츠        │
│ 상태 정보                                       │                              │
└─────────────────────────────────────────────────┴──────────────────────────────┘
```

- 데스크톱: 사이드바는 256px 고정 폭이며 콘텐츠는 `margin-left: 256px`로 배치합니다.
- 태블릿: 사이드바 폭을 232px로 축소합니다.
- 모바일: 사이드바를 일반 문서 흐름으로 전환하고 콘텐츠를 세로로 쌓습니다.
- 상단 헤더는 콘텐츠 영역 상단에 sticky로 유지해 현재 화면의 맥락을 보존합니다.

## 3. 색상 토큰

| 역할 | 토큰 | 값 | 사용처 |
|---|---|---:|---|
| 기본 텍스트 | `--ink` | `#1b1c1d` | 본문, 표, 제목 |
| 보조 텍스트 | `--muted` | `#5f656d` | 설명, 라벨, 비활성 내비게이션 |
| 주요 브랜드 | `--navy` | `#041627` | 브랜드, 주요 버튼, 활성 강조 |
| 보조 강조 | `--blue` | `#38485a` | 링크와 정보성 요소 |
| 캔버스 | `--canvas` | `#f5f3f4` | 전체 배경 |
| 표면 | `--surface` | `#fbf9fa` | 카드, 사이드바, 헤더 |
| 경계선 | `--line` | `#c4c6cd` | 카드·표·사이드바 경계 |
| 성공 | `--green` | `#027a48` | 완료, 안전, 정상 |
| 경고 | `--amber` | `#9a5b0d` | 주의, 대기 |
| 오류 | `--red` | `#ba1a1a` | 위험, 실패 |

상태 배경은 주요 색상보다 낮은 채도의 연한 톤을 사용해 정보 우선순위를 유지합니다.

## 4. 타이포그래피

- 기본 서체: `Inter`
- 한글 fallback: `Noto Sans KR`
- 제목: 18~28px, 600~700 weight
- KPI 숫자: 27px, 800 weight
- 본문: 12~14px
- 보조 라벨: 10~12px, 대문자·자간 확장 허용
- 표 데이터는 숫자 정렬과 짧은 행 높이를 우선합니다.

## 5. 컴포넌트 원칙

### 사이드바

- 워크플로우 항목은 버튼 기반 상태 내비게이션입니다.
- 활성 항목은 연한 블루 배경과 네이비 우측 경계선으로 표시합니다.
- 분석은 별도 그룹으로 묶고 `리드타임 격차`, `소진위험` 하위 링크를 제공합니다.

### 상단 헤더

- 현재 업무 맥락을 eyebrow, 제목, 상태 배지로 보여줍니다.
- 흰색에 가까운 표면과 1px 경계선으로 본문과 분리합니다.

### 카드와 KPI

- 카드 모서리는 8px, 경계선은 1px, 그림자는 낮은 농도로 통일합니다.
- KPI는 숫자·라벨·보조 설명의 3단 구조를 유지합니다.
- 색상은 수치 자체보다 의미를 표현하는 상태에 사용합니다.

### 분석 표

- 분석 화면은 공통 `AnalysisFrame`과 `DataTable`을 사용합니다.
- 헤더는 연한 회색 표면, 본문은 흰색 계열 표면을 사용합니다.
- 위험도는 `risk-badge`로 표시해 표 안에서 빠르게 인식되도록 합니다.

## 6. 구현 위치

- `app/globals.css`: 색상 토큰, 레이아웃, 반응형, 공통 컴포넌트 스타일
- `components/procurement-app.tsx`: 브랜드 영역, 워크플로우 내비게이션, 분석 하위 메뉴
- `components/analysis/*`: 분석 화면 공통 프레임과 데이터 표
- `app/analysis/*`: 분석별 페이지 콘텐츠

## 7. 사용성 및 확장 규칙

- 새 메뉴는 기존 워크플로우 내비게이션과 분석 내비게이션 중 적절한 그룹에 배치합니다.
- 색상을 새로 추가하기보다 기존 토큰을 우선 재사용합니다.
- 데이터 상태는 색상만으로 전달하지 않고 텍스트 라벨이나 배지와 함께 제공합니다.
- 표와 카드가 모바일에서 넘칠 수 있으므로 가로 스크롤 또는 세로 재배치를 제공합니다.
- 외부 폰트 로딩이 실패해도 `Inter`, `Noto Sans KR`, 시스템 sans-serif 순서로 읽을 수 있어야 합니다.
